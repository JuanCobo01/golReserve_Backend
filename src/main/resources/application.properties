spring.application.name=gol

# ================================
# Configuraci?n de la base de datos PostgreSQL
# ================================
spring.datasource.url=jdbc:postgresql://127.0.0.1:5432/gol
spring.datasource.username=postgres
spring.datasource.password=Juan1202
spring.datasource.driver-class-name=org.postgresql.Driver

# ================================
# Configuraci?n de JPA / Hibernate
# ================================
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.format_sql=true

# ================================
# Cargar datos iniciales al arrancar (solo la primera vez)
# ================================
spring.sql.init.mode=never
spring.jpa.defer-datasource-initialization=true

# ================================
# Configuracion de Cifrado ChaCha20-Poly1305
# ================================
# IMPORTANTE: En produccion, usar variables de entorno o AWS Secrets Manager
encryption.master.key=gR3$tK3y!2024#S3cur3K3yF0rCh4Ch420P0ly1305Encrypt10n

server.port=9090
